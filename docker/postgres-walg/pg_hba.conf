# Local entrypoint and health-check connections.
local   all             all                                     trust

# Password-authenticated application connections from the Compose network.
host    all             all             all                     scram-sha-256

# WAL-G uses PostgreSQL's replication protocol for physical base backups.
host    replication     all             all                     scram-sha-256

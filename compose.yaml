x-app: &app
  image: ${IMAGE_REF:-coldbrew:local}
  env_file:
    - .env

  init: true
  restart: unless-stopped
  networks:
    - internal
  depends_on:
    postgres:
      condition: service_healthy

services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: ${PGDATABASE:?Set PGDATABASE in .env}
      POSTGRES_USER: ${PGUSER:?Set PGUSER in .env}
      POSTGRES_PASSWORD: ${PGPASSWORD:?Set PGPASSWORD in .env}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\""]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    ports:
      - "127.0.0.1:${PGPORT:-5432}:5432"
    volumes:
      # PostgreSQL 18 stores its cluster in a version-specific subdirectory.
      - postgres_data:/var/lib/postgresql
    restart: unless-stopped
    networks:
      - internal

  caddy:
    image: caddy:2.10.2-alpine
    environment:
      APP_DOMAIN: ${APP_DOMAIN:?Set APP_DOMAIN in .env}
    depends_on:
      web:
        condition: service_healthy
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped
    networks:
      - internal
    mem_limit: 128m
    cpus: 0.25

  web:
    <<: *app
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app/apps/web
    command: ["bun", "/app/apps/web/.output/server/index.mjs"]
    healthcheck:
      test:
        [
          "CMD-SHELL",
          'bun -e ''fetch("http://127.0.0.1:3000/api/health").then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))''',
        ]
      interval: 15s
      timeout: 3s
      retries: 5
      start_period: 15s
    mem_limit: 512m
    cpus: 0.75

  donationalerts:
    <<: *app
    command: ["bun", "apps/donationalerts/src/index.ts"]
    mem_limit: 384m
    cpus: 0.5

  video:
    <<: *app
    command: ["bun", "apps/video/src/index.ts"]
    mem_limit: 256m
    cpus: 0.5

networks:
  internal:

volumes:
  caddy_data:
  caddy_config:
  postgres_data:
